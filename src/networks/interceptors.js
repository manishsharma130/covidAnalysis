export const responseInterceptor = (response) => response;

export const requestInterceptor = (request) => request;

export const requestErrorInterceptor = (reqErr) => reqErr;

export const responseErrorInterceptor = (rejErr) => rejErr;
