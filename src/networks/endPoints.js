const main_url = "https://covid-api.com/";
const version = "api/";

export const endPoints = {
    base_url: `${main_url}${version}`,
    regions: "regions",
    provinces: "provinces/",
    reports: "reports",
    total_reports: "reports/total"
};
